# 测试记录

日期：2026-09-13。执行者：Codex 主代理与并行子代理。
环境：macOS 15.7.9 / arm64、CPython 3.14.7、pygame-ce 2.5.8、SDL 2.32.10。

## 自动化结果

新建独立虚拟环境，只安装 `requirements.txt` 中的 pygame-ce 后，在 `homework2/` 执行：

```bash
python -m unittest discover -s tests -v
```

共 57 项测试通过：33 项规则与关卡测试、24 项界面与事件循环测试。本次运行耗时 0.243 秒，完整输出见 [test-output.txt](test-output.txt)。子测试另行覆盖多种方向、关卡和状态，不增加上述测试方法计数。

界面测试使用 SDL 虚拟显示器。另在 macOS 正常桌面会话运行 `tools/capture_screenshots.py`，用 Pygame 事件队列点选按钮和箭头，逐帧播放动画，完成三个内置关卡。新环境还从仓库外启动了 `main.py`，确认资源不依赖工作目录。

| 编号 | 输入或步骤 | 预期 | 实际结果 |
| --- | --- | --- | --- |
| T01 | 点击无阻挡箭头，分步推进动作 | 飞出后删除一次，失误不变 | 通过；结束前仍在逻辑棋盘，结束后计数减 1 |
| T02 | 点击有阻挡箭头 | 保留位置，扣 1 次并反馈 | 通过；原生窗口及截图中箭头变色，逐帧绘制晃动 |
| T03 | 上下左右四条边的朝外箭头 | 不越界，正常飞出 | 通过；覆盖负索引误读风险 |
| T04 | 清空非末关，点击下一关 | 通关后才切换，恢复新关布局和次数 | 通过；默认三关也通过界面事件走通 |
| T05 | 多次点击阻挡箭头至次数为 0 | 反馈结束才失败，棋盘锁定，可重开 | 通过；失败截图及第二关重开测试已保存 |
| T06 | 清除箭头和扣次后重开 | 当前关布局、计数和动作恢复 | 通过 |
| T07 | 四方向的空路径、相邻阻挡、隔空格阻挡；遍历阻挡朝向 | 只按占用判断，不漏查远处箭头 | 通过 |
| T08 | 在身后和其他行列放箭头 | 不误判阻挡 | 通过 |
| T09 | 空格、外部、已消除格、右键、抬起、移动和长按 | 无无效扣次或连续点选 | 通过 |
| T10 | 动画中重复点击；结束帧前向真实事件队列压入点击 | 不排队，不重复删除或扣次 | 通过；结束帧回归在修复前失败，修复后通过 |
| T11 | 两种动画中点击重开；同帧再点击新局箭头 | 旧动作和旧帧耗时不影响新局 | 通过 |
| T12 | 删除阻挡箭头后点击原箭头 | 读取当前棋盘，允许飞出 | 通过 |
| T13 | 三组 PRD 解序、贪心可解性检查、图形窗口自动通关 | 三关均可清空，四方向齐全 | 自动化通过；提交者本人试玩待完成 |
| T14 | 末关通关，重玩第一关或返回首页 | 不加载越界关卡 | 通过；两条分支都有事件测试 |
| T15 | 四角、右下外边界、格子分界线 | 左上包含，右下不包含，一次命中一格 | 通过 |
| T16 | 重复重开、切换关卡，修改传入列表 | 模板与运行局相互独立 | 通过 |
| T17 | 新环境安装、异目录启动、渲染所有状态、检查字形和布局 | 资源齐全、中文可见、状态文本不被棋盘遮挡 | 通过；仅 macOS 已验证 |
| T18 | 所有状态及两种动画中向队列发送关闭窗口事件，执行 `run()` | 退出且释放 display/font，不等待动画 | 通过 |

其他检查包括无效关卡拒绝加载、重复编号、可解性验证不修改模板、不可解布局、非法时间增量、每关独立的失误预算、错误状态下按钮操作无效。

## 画面检查与修复

主代理读取了程序生成的开始、游戏、碰撞、失败和通关 PNG，并检查第三关大棋盘的计数、按钮和文案。截图见 [screenshots](screenshots)。这是 AI 画面检查，不计作提交者本人试玩。

| Before | After |
| --- | --- |
| 开始时只有 PRD，没有游戏窗口 | `ui.py` 提供开始、游戏、失败、单关通关和全部通关画面 |
| 没有字库与图形资源 | 附带中文字体子集及许可；四方向箭头用多边形绘制 |
| 大棋盘遮住上方箭头计数 | 计数改为横向排列；加入所有内置棋盘与状态文本不相交的回归测试 |
| 动画结束帧先计时再处理事件，旧点击可能成为新动作 | 先处理事件，再推进仍然有效的旧动作；主循环与截图检查使用同一个 `step` |

动画按经过时间更新，主循环以 60 FPS 为目标，不通过阻塞休眠播放动作。实际窗口已运行完整自动流程；未作长期帧率或其他平台性能测试。

## 提交者本人试玩

请本人启动 `python main.py`，逐关完成通关和重开，观察动画、文字和鼠标反馈，再填写此表。自动化通过不替代此项。

| 关卡 | 本人通关 | 本关重开 | 日期与说明 |
| --- | --- | --- | --- |
| 1：认识阻挡 | 待完成 | 待完成 | 待填写 |
| 2：交叉观察 | 待完成 | 待完成 | 待填写 |
| 3：连续解锁 | 待完成 | 待完成 | 待填写 |

博客不在本次任务范围内。
